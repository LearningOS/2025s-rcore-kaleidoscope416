增加了系统调用trace，在tcb中加入一个数组task_syscall_count,大小为5，对应已有的系统调用个数，用来记录次数，同时，为tcb实现了计数和打印的方法来输出计数结果    
# 问题
1   
（）    
2   
1）用户栈的栈顶地址；trap之后的恢复上下文，或者在应用初始化时restore一个构造出来的特殊上下文   
2）将sstatus，sepc，sscratch等CSR从上下文中恢复；sstatus保存发生系统调用前cpu特权级的状态，帮助CSR设立特权级，sepc保存着系统调用前下一条指令的地址，帮助程序计数器继续运行，sscratch保存用户栈的栈顶地址，帮助恢复用户栈    
3）x2寄存器保存着sp，自然不用存，x4寄存器，除非我们手动出于一些特殊用途使用它，一般会被用到     
4）sp指向内核栈，sscaratch指向用户栈
5）sret，这是一条特权指令，表示从 S 模式返回到 U 模式
6）ecall